# Iterate through numbers from 0 to 4 using the range function
for num in range(5):
    # If the current number is 2, skip this iteration
    if num == 2:
        continue
    # Print the current number
    print(num)
