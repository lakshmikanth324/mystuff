# Define a function `greet` that takes a parameter `name`
def greet(name):
    # Print a greeting message using an f-string
    print(f"Hello, {name}!")

# Call the `greet` function with the argument "Alice"
greet("Alice")
