# Iterate through numbers from 0 to 9 using the range function
for num in range(10):
    # If the current number is 5, exit the loop
    if num == 5:
        break
    # Print the current number
    print(num)
