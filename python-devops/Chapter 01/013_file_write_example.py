# Open the file 'example.txt' in write mode
file = open("example.txt", "w")

# Write the string "Hello, World!" to the file
file.write("Hello, World!")

# Close the file to ensure all data is saved and the file is properly closed
file.close()
